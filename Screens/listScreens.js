import React from "react";
import { View, Text, StyleSheet, FlatList } from "react-native";

export default ListScreen = () => {
 const friends = [
      {name:'attiya'},
      {name:'Hamza'},
      {name:'Hassan'},
      {name:'Abadi'},


 ];
 
    // return <FlatList data={friends} re
}

const styles = StyleSheet.create({});